[package]
name = "api-server"
version = "0.1.0"
edition = "2024"

[dependencies]

sqlx = {workspace = true}
dotenvy = { workspace = true}
tokio = { version = "1.51.1", features = ["full"]}
axum = {version = "0.8.8"}